	<h1><%= t('.header') %></h1>
<% if @vacancies.count > 0 %>
	<table class="test_sort">
	  <tr>
	    <th><%= t('.title') %></th>
	    <th><%= t('.conditions') %></th>
	    <th><%= t('.requirements') %></th>
	    <th><%= t('.city') %></th>
	    <th><%= t('.salary') %></th>
			<th><%= t('.priority') %></th>
			<th></th>
	    <th></th>
	    <th></th>
	  </tr>
	<%= render @vacancies %>
	</table>
	<br />

	<div class="vacancies_left_info">
		<%= render 'vacancies_left_info' %>
	</div>
	<br />
	<%= will_paginate @vacancies %>
<% else %>
<p>Вакансий пока нет... Создайте хотя бы одну.</p>
<% end %>